// Import the 'User' and 'Thought' models from the js file
const User = require("./User");
const Thought = require("./Thought");

// Export the models as an object
module.exports = { User, Thought };
